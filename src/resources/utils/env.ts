import 'dotenv/config';

export const env = { // Not needed for this framework but kept for reference
    baseUrl: process.env.BASE_URL!,
    username: process.env.USERNAME!,
    password: process.env.PASSWORD!
}